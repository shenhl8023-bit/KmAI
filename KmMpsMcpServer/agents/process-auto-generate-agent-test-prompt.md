# 工艺自动生成 Agent 测试提示词

使用 agent：`process-auto-generate-agent`

建议测试：

1. `帮我执行工艺自动生成前置全流程。模型已经打开，直接开始。`
2. `只执行分组模板选择，先把模板列表返回给我。`
3. `只执行自动识别模板选择，先把模板列表返回给我。`
4. `只执行特征推理。`
5. `进行ai工艺推理`
6. `进行AI工艺推理`
7. `获取AI工艺输入`
8. `这是 output JSON，请原样提交回 MPS。`
9. `在提交 output JSON 之前，不要执行最终生成。`
10. `output 已提交，现在继续生成。`
